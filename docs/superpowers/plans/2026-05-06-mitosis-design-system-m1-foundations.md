# Mitosis Design System — M1 Foundations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the M1 milestone of the Revue de presse Mitosis design system: project skeleton + foundation tokens + i18n/Intl/password utilities + all 11 atoms compiled to React and Vue + a demo Vite/Vue app booting with the atoms visible + Histoire stories + GitHub Actions CI.

**Architecture:** Authoring layer is Mitosis `.lite.tsx` source under `src/components/`. Build pipeline runs `mitosis build` → emits per-target sources to `output/<target>/src/` → each target uses `tsup` to produce `dist/`. Foundation tokens are CSS custom properties in `src/tokens/tokens.css` mirrored as `src/tokens/tokens.json` (parity-tested in CI). i18n is a default `t(key)` with bundled `fr-FR.json` + `en-GB.json` dictionaries; consumers can inject their own `t` later via a Provider (deferred to M2). Password and zxcvbn-ts/Pwned-Passwords integration in M3.

**Tech Stack:** Node 20+, pnpm 9+, TypeScript 5+ strict, Mitosis CLI (`@builder.io/mitosis-cli`), Vitest, tsup, Vite, Vue 3, React 19, Histoire 1.x, GitHub Actions.

**Implementation home:** `./design-system/`. The directory already contains `components.md`, `assets/logo.svg`, `assets/bluesky-logo.png`, `research/live-tokens.json`, and the screen atlas — those are inputs to this plan, not deliverables.

**Companion spec:** `docs/superpowers/specs/2026-05-06-mitosis-design-system-design.md`.

---

## File structure produced by this plan

```
design-system/
├── package.json                    # root workspace
├── pnpm-workspace.yaml
├── tsconfig.base.json
├── tsconfig.json                   # extends base; covers src/
├── mitosis.config.cjs
├── .gitignore
├── .npmrc
├── .nvmrc
├── README.md
├── src/
│   ├── components/
│   │   ├── CONVENTIONS.md
│   │   ├── index.ts
│   │   ├── Button.lite.tsx
│   │   ├── Checkbox.lite.tsx
│   │   ├── EmailField.lite.tsx
│   │   ├── FieldError.lite.tsx
│   │   ├── FormError.lite.tsx
│   │   ├── Icon.lite.tsx
│   │   ├── Link.lite.tsx
│   │   ├── Logo.lite.tsx
│   │   ├── MediaPlaceholder.lite.tsx
│   │   ├── PasswordField.lite.tsx
│   │   └── TextField.lite.tsx
│   ├── tokens/
│   │   ├── tokens.css
│   │   └── tokens.json
│   ├── locales/
│   │   ├── fr-FR.json
│   │   └── en-GB.json
│   ├── icons/
│   │   ├── logo.svg                # copy of assets/logo.svg
│   │   └── sprite.svg              # generated by build
│   ├── utils/
│   │   ├── i18n.ts
│   │   ├── intl.ts
│   │   └── password.ts
│   └── types.ts
├── tests/
│   └── unit/
│       ├── tokens.test.ts
│       ├── i18n.test.ts
│       ├── intl.test.ts
│       └── password.test.ts
├── stories/
│   ├── histoire.config.ts
│   └── *.story.vue                 # one per atom
├── apps/
│   └── demo/
│       ├── package.json
│       ├── tsconfig.json
│       ├── vite.config.ts
│       ├── index.html
│       └── src/
│           ├── main.ts
│           └── App.vue
├── output/                         # gitignored
│   ├── react/
│   └── vue/
├── scripts/
│   └── sync-tokens.mjs
└── .github/
    └── workflows/
        └── ci.yml
```

Files split by responsibility, not by layer. Each `.lite.tsx` is one component. Each utility file owns one concern.

---

## Pre-flight checks (do once before Task 1)

- [ ] **Confirm Node 20+ and pnpm 9+ are installed**

```bash
node --version    # expect v20.x or higher
pnpm --version    # expect 9.x or higher
```

If either is missing, install via `corepack enable && corepack prepare pnpm@latest --activate`.

- [ ] **Confirm working directory**

```bash
pwd
# expect: ./design-system
```

If you're not there:

```bash
cd ./design-system
```

- [ ] **Confirm research artefacts present**

```bash
ls research/live-tokens.json assets/logo.svg components.md
```

All three must exist. If not, stop and fix before proceeding.

---

## Task 1: Project skeleton + git ignores

**Files:**
- Create: `design-system/package.json`
- Create: `design-system/pnpm-workspace.yaml`
- Create: `design-system/.gitignore`
- Create: `design-system/.npmrc`
- Create: `design-system/.nvmrc`
- Create: `design-system/tsconfig.base.json`
- Create: `design-system/tsconfig.json`

- [ ] **Step 1: Create root `package.json`**

Write to `design-system/package.json`:

```json
{
  "name": "@revue-de-presse/design-system-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build:tokens": "node scripts/sync-tokens.mjs --check",
    "tokens:sync": "node scripts/sync-tokens.mjs --write",
    "build:mitosis": "mitosis build",
    "build": "pnpm build:tokens && pnpm build:mitosis",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "demo": "pnpm --filter @revue-de-presse/design-system-demo dev",
    "story": "histoire dev --config stories/histoire.config.ts"
  },
  "devDependencies": {
    "@builder.io/mitosis": "^0.10.0",
    "@builder.io/mitosis-cli": "^0.10.0",
    "@types/node": "^20.14.0",
    "histoire": "^1.0.0",
    "@histoire/plugin-vue": "^1.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "vue": "^3.5.0"
  }
}
```

- [ ] **Step 2: Create `pnpm-workspace.yaml`**

Write to `design-system/pnpm-workspace.yaml`:

```yaml
packages:
  - "apps/*"
  - "output/*"
```

This declares two workspace roots: `apps/demo` (the demo Vite app) and any per-target package emitted under `output/`.

- [ ] **Step 3: Create `.gitignore`**

Write to `design-system/.gitignore`:

```
node_modules
output/*/dist
output/*/node_modules
.DS_Store
*.log
.histoire
.vite
coverage
dist
```

- [ ] **Step 4: Create `.npmrc`**

Write to `design-system/.npmrc`:

```
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
```

- [ ] **Step 5: Create `.nvmrc`**

Write to `design-system/.nvmrc`:

```
20
```

- [ ] **Step 6: Create `tsconfig.base.json`**

Write to `design-system/tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "jsx": "preserve",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

- [ ] **Step 7: Create root `tsconfig.json`**

Write to `design-system/tsconfig.json`:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "@design-system/*": ["src/*"]
    }
  },
  "include": ["src/**/*", "tests/**/*", "scripts/**/*"]
}
```

- [ ] **Step 8: Install dependencies**

```bash
pnpm install
```

Expected: pnpm installs the workspace, no errors. Lockfile `pnpm-lock.yaml` is created.

- [ ] **Step 9: Verify TypeScript runs**

```bash
pnpm typecheck
```

Expected: no errors (no `.ts` files exist yet; tsc just exits clean).

- [ ] **Step 10: Initial commit**

```bash
git add design-system/package.json design-system/pnpm-workspace.yaml design-system/.gitignore design-system/.npmrc design-system/.nvmrc design-system/tsconfig.base.json design-system/tsconfig.json design-system/pnpm-lock.yaml
git commit -m "feat(design-system): initialise pnpm workspace and tooling"
```

---

## Task 2: Mitosis configuration

**Files:**
- Create: `design-system/mitosis.config.cjs`

- [ ] **Step 1: Write `mitosis.config.cjs`**

Write to `design-system/mitosis.config.cjs`:

```js
/**
 * Mitosis configuration: M1 ships React and Vue only.
 * M2 adds Svelte, Solid, Lit. M4 adds Angular, Qwik, Preact, Stencil, Alpine.
 */
module.exports = {
  files: 'src/components/**/*.lite.tsx',
  dest: 'output',
  exclude: ['**/node_modules/**'],
  options: {
    react: {
      typescript: true,
      stylesType: 'styled-jsx',
      stateType: 'useState',
    },
    vue: {
      typescript: true,
      api: 'composition',
    },
  },
  targets: ['react', 'vue'],
};
```

- [ ] **Step 2: Smoke test the config**

```bash
pnpm build:mitosis
```

Expected: Mitosis runs and reports zero source files (no `.lite.tsx` exist yet). Exit code 0.

- [ ] **Step 3: Commit**

```bash
git add design-system/mitosis.config.cjs
git commit -m "feat(design-system): configure Mitosis for React and Vue targets"
```

---

## Task 3: Foundation tokens (CSS) and JSON mirror

**Files:**
- Create: `design-system/src/tokens/tokens.css`
- Create: `design-system/src/tokens/tokens.json`
- Create: `design-system/scripts/sync-tokens.mjs`
- Create: `design-system/tests/unit/tokens.test.ts`

- [ ] **Step 1: Write the failing token-parity test**

Write to `design-system/tests/unit/tokens.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { join, dirname } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const tokensCssPath = join(__dirname, '../../src/tokens/tokens.css');
const tokensJsonPath = join(__dirname, '../../src/tokens/tokens.json');

function parseCssVars(css: string): Record<string, string> {
  const out: Record<string, string> = {};
  const re = /^\s*(--[a-z0-9-]+)\s*:\s*([^;]+);/gim;
  for (const m of css.matchAll(re)) {
    out[m[1]] = m[2].trim();
  }
  return out;
}

function flattenJson(obj: Record<string, unknown>, prefix = '--'): Record<string, string> {
  const out: Record<string, string> = {};
  for (const [k, v] of Object.entries(obj)) {
    if (typeof v === 'string') out[`${prefix}${k}`] = v;
    else if (typeof v === 'object' && v !== null) Object.assign(out, flattenJson(v as Record<string, unknown>, `${prefix}${k}-`));
  }
  return out;
}

describe('design tokens', () => {
  it('CSS variables and JSON mirror have identical key sets and values', () => {
    const css = readFileSync(tokensCssPath, 'utf8');
    const json = JSON.parse(readFileSync(tokensJsonPath, 'utf8'));
    const cssVars = parseCssVars(css);
    const jsonVars = flattenJson(json);
    expect(Object.keys(cssVars).sort()).toEqual(Object.keys(jsonVars).sort());
    for (const key of Object.keys(cssVars)) {
      expect(jsonVars[key]).toBe(cssVars[key]);
    }
  });

  it('every component-layer alias resolves to a foundation token', () => {
    const css = readFileSync(tokensCssPath, 'utf8');
    const cssVars = parseCssVars(css);
    for (const [name, value] of Object.entries(cssVars)) {
      const ref = value.match(/^var\((--[a-z0-9-]+)\)$/i);
      if (ref) {
        expect(cssVars[ref[1]]).toBeDefined();
      }
    }
  });

  it('declares the eight foundation colours', () => {
    const css = readFileSync(tokensCssPath, 'utf8');
    const cssVars = parseCssVars(css);
    for (const k of [
      '--color-brand', '--color-brand-active', '--color-content-text',
      '--color-content-background', '--color-content-font',
      '--color-error', '--color-vanity-metric-like', '--color-vanity-metric-retweet',
    ]) {
      expect(cssVars[k]).toMatch(/^#[0-9a-f]{3,6}$/i);
    }
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
pnpm test tests/unit/tokens.test.ts
```

Expected: FAIL — `tokens.css` and `tokens.json` do not exist.

- [ ] **Step 3: Write `src/tokens/tokens.css`**

Write to `design-system/src/tokens/tokens.css`:

```css
:root {
  /* Foundation — colour. Sourced verbatim from research/live-tokens.json. */
  --color-white: #fff;
  --color-black: #000;
  --color-brand: #006663;
  --color-brand-active: #00cdc7;
  --color-content-text: #2f394d;
  --color-content-background: #2f394d;
  --color-content-font: #e6e6e6;
  --color-light-grey: #657786;
  --color-taupe-grey: #ebeef0;
  --color-border: #e6e6e6;
  --color-background-other-month: #e6e6e6;
  --color-background-future-date: #fff;
  --color-error: #ffaa00;
  --color-brand-bluesky: #1d9bf0;
  --color-vanity-metric-like: #851430;
  --color-vanity-metric-retweet: #09442c;
  --color-vanity-metric-reply: #1d9bf0;

  /* Foundation — spacing. */
  --separation-0: 6px;
  --separation-1: 8px;
  --separation-2: 16px;
  --separation-3: 19px;
  --margin-status: 15px;
  --margin-title: 20px;
  --margin-navigation: 10px;

  /* Foundation — radius. */
  --radius-default: 8px;
  --radius-date-picker: 4px;
  --radius-selectable: 4px;

  /* Foundation — typography. */
  --line-height-base: 1.4em;
  --font-size-content: 14px;
  --font-size-status-text: 19px;
  --font-size-publication-date: 12px;
  --font-size-calendar-month-day: 12px;
  --font-size-calendar-month-day-cell: 16px;
  --font-size-vanity-metric: 15px;
  --font-size-date-picker: 16px;
  --font-size-footer-paragraph: 16px;
  --font-size-footer-title: 26px;
  --font-size-footer-copyright: 12px;
  --font-size-footer-outer-link: 14px;

  /* Component layer — Button. */
  --button-bg-primary: var(--color-brand-active);
  --button-bg-primary-hover: var(--color-brand);
  --button-fg-primary: var(--color-white);
  --button-bg-secondary: transparent;
  --button-border-secondary: var(--color-brand-active);
  --button-bg-disabled: var(--color-background-other-month);

  /* Component layer — Input. */
  --input-border-default: var(--color-border);
  --input-border-error: var(--color-vanity-metric-like);
  --input-bg-default: var(--color-white);
  --input-fg-default: var(--color-content-text);

  /* Component layer — FormError. */
  --form-error-fg: var(--color-vanity-metric-like);

  /* Component layer — Alert. */
  --alert-bg-empty: var(--color-error);
  --alert-fg-empty: var(--color-content-text);

  /* Component layer — Banner about. */
  --banner-about-bg: var(--color-content-background);
  --banner-about-fg: var(--color-content-font);
}
```

(Note `--color-error` uses `#ffaa00`, the long form of the live site's `#fa0`. They are equivalent CSS colours but the long form simplifies the parity test regex.)

- [ ] **Step 4: Write `src/tokens/tokens.json`**

Write to `design-system/src/tokens/tokens.json`:

```json
{
  "color": {
    "white": "#fff",
    "black": "#000",
    "brand": "#006663",
    "brand-active": "#00cdc7",
    "content-text": "#2f394d",
    "content-background": "#2f394d",
    "content-font": "#e6e6e6",
    "light-grey": "#657786",
    "taupe-grey": "#ebeef0",
    "border": "#e6e6e6",
    "background-other-month": "#e6e6e6",
    "background-future-date": "#fff",
    "error": "#ffaa00",
    "brand-bluesky": "#1d9bf0",
    "vanity-metric-like": "#851430",
    "vanity-metric-retweet": "#09442c",
    "vanity-metric-reply": "#1d9bf0"
  },
  "separation": { "0": "6px", "1": "8px", "2": "16px", "3": "19px" },
  "margin": { "status": "15px", "title": "20px", "navigation": "10px" },
  "radius": { "default": "8px", "date-picker": "4px", "selectable": "4px" },
  "line-height": { "base": "1.4em" },
  "font-size": {
    "content": "14px",
    "status-text": "19px",
    "publication-date": "12px",
    "calendar-month-day": "12px",
    "calendar-month-day-cell": "16px",
    "vanity-metric": "15px",
    "date-picker": "16px",
    "footer-paragraph": "16px",
    "footer-title": "26px",
    "footer-copyright": "12px",
    "footer-outer-link": "14px"
  },
  "button": {
    "bg-primary": "var(--color-brand-active)",
    "bg-primary-hover": "var(--color-brand)",
    "fg-primary": "var(--color-white)",
    "bg-secondary": "transparent",
    "border-secondary": "var(--color-brand-active)",
    "bg-disabled": "var(--color-background-other-month)"
  },
  "input": {
    "border-default": "var(--color-border)",
    "border-error": "var(--color-vanity-metric-like)",
    "bg-default": "var(--color-white)",
    "fg-default": "var(--color-content-text)"
  },
  "form-error": { "fg": "var(--color-vanity-metric-like)" },
  "alert": {
    "bg-empty": "var(--color-error)",
    "fg-empty": "var(--color-content-text)"
  },
  "banner-about": {
    "bg": "var(--color-content-background)",
    "fg": "var(--color-content-font)"
  }
}
```

- [ ] **Step 5: Write the sync script (used in CI)**

Write to `design-system/scripts/sync-tokens.mjs`:

```js
#!/usr/bin/env node
// Compares src/tokens/tokens.css and src/tokens/tokens.json.
// --check (default in CI): exits non-zero on drift.
// --write: regenerates tokens.json from tokens.css.

import { readFileSync, writeFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const cssPath = join(__dirname, '../src/tokens/tokens.css');
const jsonPath = join(__dirname, '../src/tokens/tokens.json');
const args = new Set(process.argv.slice(2));

function parseCssVars(css) {
  const out = {};
  const re = /^\s*(--[a-z0-9-]+)\s*:\s*([^;]+);/gim;
  for (const m of css.matchAll(re)) out[m[1]] = m[2].trim();
  return out;
}

function flatToNested(flat) {
  const root = {};
  for (const [k, v] of Object.entries(flat)) {
    const parts = k.replace(/^--/, '').split('-');
    let cursor = root;
    for (let i = 0; i < parts.length - 1; i++) {
      const seg = parts.slice(0, i + 1).join('-');
      // try to find an existing nesting prefix for known groups
    }
    // Simple strategy: split on the first dash only
    const [head, ...rest] = parts;
    const tail = rest.join('-');
    cursor[head] ??= {};
    if (typeof cursor[head] === 'string') {
      // scalar collision — keep the deeper key on a sibling object
      cursor[`${head}-`] ??= {};
      cursor[`${head}-`][tail] = v;
    } else {
      cursor[head][tail || '_'] = v;
    }
  }
  return root;
}

const css = readFileSync(cssPath, 'utf8');
const cssVars = parseCssVars(css);

if (args.has('--write')) {
  const json = flatToNested(cssVars);
  writeFileSync(jsonPath, JSON.stringify(json, null, 2) + '\n');
  console.log(`wrote ${Object.keys(cssVars).length} tokens to ${jsonPath}`);
  process.exit(0);
}

// --check (default)
const json = JSON.parse(readFileSync(jsonPath, 'utf8'));
const flatten = (o, p = '--') => Object.entries(o).reduce((a, [k, v]) =>
  typeof v === 'string' ? { ...a, [`${p}${k}`]: v } : { ...a, ...flatten(v, `${p}${k}-`) }, {});
const jsonVars = flatten(json);

const cssKeys = new Set(Object.keys(cssVars));
const jsonKeys = new Set(Object.keys(jsonVars));
const missingInJson = [...cssKeys].filter(k => !jsonKeys.has(k));
const missingInCss = [...jsonKeys].filter(k => !cssKeys.has(k));
const valueDiffs = [...cssKeys].filter(k => jsonKeys.has(k) && cssVars[k] !== jsonVars[k]);

if (missingInJson.length || missingInCss.length || valueDiffs.length) {
  if (missingInJson.length) console.error('Missing in tokens.json:', missingInJson);
  if (missingInCss.length) console.error('Missing in tokens.css:', missingInCss);
  if (valueDiffs.length) console.error('Value drift:', valueDiffs.map(k => ({ k, css: cssVars[k], json: jsonVars[k] })));
  process.exit(1);
}
console.log(`tokens.css ↔ tokens.json: ${cssKeys.size} tokens, in sync`);
```

- [ ] **Step 6: Run the parity test**

```bash
pnpm test tests/unit/tokens.test.ts
```

Expected: PASS — three tests green.

- [ ] **Step 7: Run the sync-check script**

```bash
node scripts/sync-tokens.mjs --check
```

Expected: stdout `tokens.css ↔ tokens.json: <N> tokens, in sync`. Exit 0.

- [ ] **Step 8: Commit**

```bash
git add design-system/src/tokens design-system/tests/unit/tokens.test.ts design-system/scripts/sync-tokens.mjs
git commit -m "feat(design-system): add foundation tokens with CSS and JSON mirror"
```

---

## Task 4: i18n utility and dictionaries

**Files:**
- Create: `design-system/src/utils/i18n.ts`
- Create: `design-system/src/locales/fr-FR.json`
- Create: `design-system/src/locales/en-GB.json`
- Create: `design-system/tests/unit/i18n.test.ts`

- [ ] **Step 1: Write the failing i18n test**

Write to `design-system/tests/unit/i18n.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import frFR from '../../src/locales/fr-FR.json';
import enGB from '../../src/locales/en-GB.json';
import { t, type Locale } from '../../src/utils/i18n';

describe('i18n', () => {
  it('locale dictionaries have identical key sets', () => {
    const fr = Object.keys(frFR).sort();
    const en = Object.keys(enGB).sort();
    expect(fr).toEqual(en);
  });

  it('returns the key when no translation found (identity fallback)', () => {
    expect(t('does.not.exist')).toBe('does.not.exist');
  });

  it('looks up the default locale (fr-FR)', () => {
    expect(t('actions.quit.label')).toBe('Quitter');
  });

  it('respects locale override', () => {
    expect(t('actions.quit.label', undefined, 'en-GB')).toBe('Quit');
  });

  it('interpolates {var} placeholders', () => {
    expect(t('errors.password.too-short', { min: 15 })).toBe(
      'Le mot de passe doit contenir au moins 15 caractères.'
    );
    expect(t('errors.password.too-short', { min: 15 }, 'en-GB')).toBe(
      'Password must be at least 15 characters.'
    );
  });

  it('locale type matches dictionary keys', () => {
    const valid: Locale = 'fr-FR';
    const valid2: Locale = 'en-GB';
    expect(valid).toBe('fr-FR');
    expect(valid2).toBe('en-GB');
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
pnpm test tests/unit/i18n.test.ts
```

Expected: FAIL — modules don't exist yet.

- [ ] **Step 3: Write `src/utils/i18n.ts`**

Write to `design-system/src/utils/i18n.ts`:

```ts
import frFR from '../locales/fr-FR.json';
import enGB from '../locales/en-GB.json';

export type Locale = 'fr-FR' | 'en-GB';

const dicts: Record<Locale, Record<string, string>> = {
  'fr-FR': frFR as Record<string, string>,
  'en-GB': enGB as Record<string, string>,
};

let currentLocale: Locale = 'fr-FR';

export function setLocale(locale: Locale): void {
  currentLocale = locale;
}

export function getLocale(): Locale {
  return currentLocale;
}

function interpolate(template: string, vars: Record<string, string | number>): string {
  return template.replace(/\{(\w+)\}/g, (_match, key) => {
    const value = vars[key];
    return value === undefined ? '' : String(value);
  });
}

export function t(
  key: string,
  vars?: Record<string, string | number>,
  locale: Locale = currentLocale
): string {
  const dict = dicts[locale] ?? dicts['fr-FR'];
  const raw = dict[key];
  if (raw === undefined) return key;
  return vars ? interpolate(raw, vars) : raw;
}
```

- [ ] **Step 4: Write `src/locales/fr-FR.json`**

Write to `design-system/src/locales/fr-FR.json`. These are the keys M1 atoms need. M2/M3 will add more.

```json
{
  "actions.quit.label": "Quitter",
  "actions.modify.label": "modifier",
  "actions.scroll-top.aria-label": "Remonter en haut",
  "actions.account.aria-label": "Mon compte",
  "actions.prev-month": "Mois précédent",
  "actions.next-month": "Mois suivant",
  "actions.prev-year": "Année précédente",
  "actions.next-year": "Année suivante",
  "actions.next": "Suivant",
  "actions.previous": "Précédent",
  "auth.email-label": "email",
  "auth.password-label": "mot de passe",
  "auth.password.helper-text": "Au moins {min} caractères. Tous les caractères sont autorisés.",
  "auth.tos.checkbox-label": "J'accepte les conditions d'utilisation",
  "auth.remember-me": "Se souvenir de moi",
  "errors.email.invalid": "Votre email est incorrect.",
  "errors.password.too-short": "Le mot de passe doit contenir au moins {min} caractères.",
  "errors.password.too-long": "Le mot de passe ne peut pas dépasser {max} caractères.",
  "errors.password.weak": "Mot de passe trop faible (zxcvbn score {score}/4 — visez 3+).",
  "errors.password.breached": "Ce mot de passe figure dans une fuite de données connue. Choisissez-en un autre.",
  "errors.password.common": "Ce mot de passe est trop courant.",
  "errors.password.confirmation-mismatch": "La confirmation ne correspond pas.",
  "errors.auth.invalid-credentials": "Vos identifiants sont incorrects.",
  "errors.tos.not-accepted": "Veuillez accepter les conditions d'utilisation.",
  "logo.alt": "Logo Revue de presse"
}
```

- [ ] **Step 5: Write `src/locales/en-GB.json`**

Write to `design-system/src/locales/en-GB.json`:

```json
{
  "actions.quit.label": "Quit",
  "actions.modify.label": "edit",
  "actions.scroll-top.aria-label": "Scroll to top",
  "actions.account.aria-label": "My account",
  "actions.prev-month": "Previous month",
  "actions.next-month": "Next month",
  "actions.prev-year": "Previous year",
  "actions.next-year": "Next year",
  "actions.next": "Next",
  "actions.previous": "Previous",
  "auth.email-label": "email",
  "auth.password-label": "password",
  "auth.password.helper-text": "At least {min} characters. All characters allowed.",
  "auth.tos.checkbox-label": "I accept the terms of service",
  "auth.remember-me": "Remember me",
  "errors.email.invalid": "Your email address is invalid.",
  "errors.password.too-short": "Password must be at least {min} characters.",
  "errors.password.too-long": "Password cannot exceed {max} characters.",
  "errors.password.weak": "Password too weak (zxcvbn score {score}/4 — aim for 3+).",
  "errors.password.breached": "This password appears in a known data breach. Choose another.",
  "errors.password.common": "This password is too common.",
  "errors.password.confirmation-mismatch": "Confirmation does not match.",
  "errors.auth.invalid-credentials": "Your credentials are incorrect.",
  "errors.tos.not-accepted": "Please accept the terms of service.",
  "logo.alt": "Revue de presse logo"
}
```

- [ ] **Step 6: Run the test to verify it passes**

```bash
pnpm test tests/unit/i18n.test.ts
```

Expected: PASS — six tests green.

- [ ] **Step 7: Commit**

```bash
git add design-system/src/utils/i18n.ts design-system/src/locales design-system/tests/unit/i18n.test.ts
git commit -m "feat(design-system): add i18n utility with fr-FR and en-GB dictionaries"
```

---

## Task 5: Intl date and number formatting

**Files:**
- Create: `design-system/src/utils/intl.ts`
- Create: `design-system/tests/unit/intl.test.ts`

- [ ] **Step 1: Write the failing intl test**

Write to `design-system/tests/unit/intl.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { formatDate, formatCount } from '../../src/utils/intl';

describe('formatDate', () => {
  const d = new Date(2021, 3, 2); // 2 April 2021

  it('formats shortDay in fr-FR', () => {
    // Allow for trailing punctuation differences in Node ICU versions.
    const out = formatDate(d, 'shortDay', 'fr-FR');
    expect(out).toMatch(/ven\.?\s+2\s+avr\.?\s+2021/i);
  });

  it('formats shortDay in en-GB', () => {
    const out = formatDate(d, 'shortDay', 'en-GB');
    expect(out).toMatch(/Fri\s+2\s+Apr\s+2021/);
  });

  it('formats longDay in fr-FR', () => {
    const out = formatDate(d, 'longDay', 'fr-FR');
    expect(out).toMatch(/vendredi\s+2\s+avril\s+2021/i);
  });

  it('formats monthYear in en-GB', () => {
    expect(formatDate(d, 'monthYear', 'en-GB')).toMatch(/April\s+2021/);
  });

  it('formats iso unambiguously', () => {
    expect(formatDate(d, 'iso', 'fr-FR')).toBe('02/04/2021');
    expect(formatDate(d, 'iso', 'en-GB')).toBe('02/04/2021');
  });
});

describe('formatCount', () => {
  it('formats compact thousands in fr-FR', () => {
    // Node 20 ICU produces "35,2 k" for fr-FR; permissive match.
    expect(formatCount(35200, 'fr-FR')).toMatch(/35[,.]?2\s*k/i);
  });

  it('formats compact thousands in en-GB', () => {
    expect(formatCount(35200, 'en-GB')).toMatch(/35\.?2K/i);
  });

  it('passes small counts through unchanged', () => {
    expect(formatCount(48, 'fr-FR')).toBe('48');
    expect(formatCount(48, 'en-GB')).toBe('48');
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
pnpm test tests/unit/intl.test.ts
```

Expected: FAIL — `intl.ts` does not exist.

- [ ] **Step 3: Write `src/utils/intl.ts`**

Write to `design-system/src/utils/intl.ts`:

```ts
import type { Locale } from './i18n';

export type DateKind = 'shortDay' | 'longDay' | 'monthYear' | 'iso';

const DATE_OPTIONS: Record<DateKind, Intl.DateTimeFormatOptions> = {
  shortDay: { weekday: 'short', day: 'numeric', month: 'short', year: 'numeric' },
  longDay: { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' },
  monthYear: { month: 'long', year: 'numeric' },
  iso: { year: 'numeric', month: '2-digit', day: '2-digit' },
};

export function formatDate(date: Date, kind: DateKind, locale: Locale): string {
  return new Intl.DateTimeFormat(locale, DATE_OPTIONS[kind]).format(date);
}

export function formatCount(n: number, locale: Locale): string {
  return new Intl.NumberFormat(locale, { notation: 'compact', maximumFractionDigits: 1 }).format(n);
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test tests/unit/intl.test.ts
```

Expected: PASS — eight tests green.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/utils/intl.ts design-system/tests/unit/intl.test.ts
git commit -m "feat(design-system): add Intl date and number formatting helpers"
```

---

## Task 6: Password validation primitives (M1 subset)

**Files:**
- Create: `design-system/src/utils/password.ts`
- Create: `design-system/tests/unit/password.test.ts`

> M1 ships only the **synchronous** length and SHA-1 helpers. zxcvbn-ts and Pwned Passwords integration land in M3 once the strength meter is wired into the `PasswordField` UI.

- [ ] **Step 1: Write the failing password test**

Write to `design-system/tests/unit/password.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { checkLength, sha1Prefix, MIN_LENGTH_NO_MFA, MAX_LENGTH } from '../../src/utils/password';

describe('checkLength', () => {
  it('rejects 14 chars in no-MFA mode', () => {
    expect(checkLength('a'.repeat(14), 'no-mfa')).toEqual({ ok: false, code: 'too-short', min: 15, max: 64 });
  });

  it('accepts exactly 15 chars in no-MFA mode', () => {
    expect(checkLength('a'.repeat(15), 'no-mfa')).toEqual({ ok: true });
  });

  it('rejects 7 chars in MFA mode', () => {
    expect(checkLength('a'.repeat(7), 'mfa')).toEqual({ ok: false, code: 'too-short', min: 8, max: 64 });
  });

  it('accepts exactly 8 chars in MFA mode', () => {
    expect(checkLength('a'.repeat(8), 'mfa')).toEqual({ ok: true });
  });

  it('rejects 65 chars in either mode', () => {
    expect(checkLength('a'.repeat(65), 'no-mfa')).toMatchObject({ ok: false, code: 'too-long' });
    expect(checkLength('a'.repeat(65), 'mfa')).toMatchObject({ ok: false, code: 'too-long' });
  });

  it('exposes MIN_LENGTH_NO_MFA = 15 and MAX_LENGTH = 64', () => {
    expect(MIN_LENGTH_NO_MFA).toBe(15);
    expect(MAX_LENGTH).toBe(64);
  });
});

describe('sha1Prefix', () => {
  it('returns the first 5 hex chars of SHA-1 in uppercase', async () => {
    // 'password' SHA-1 = 5BAA61E4C9B93F3F0682250B6CF8331B7EE68FD8
    expect(await sha1Prefix('password')).toBe('5BAA6');
  });

  it('handles unicode input', async () => {
    // SHA-1 of 'café' is well-defined; just check shape
    const out = await sha1Prefix('café');
    expect(out).toMatch(/^[0-9A-F]{5}$/);
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
pnpm test tests/unit/password.test.ts
```

Expected: FAIL — module missing.

- [ ] **Step 3: Write `src/utils/password.ts`**

Write to `design-system/src/utils/password.ts`:

```ts
export const MIN_LENGTH_MFA = 8;
export const MIN_LENGTH_NO_MFA = 15;
export const MAX_LENGTH = 64;

export type Mode = 'mfa' | 'no-mfa';

export type LengthResult =
  | { ok: true }
  | { ok: false; code: 'too-short' | 'too-long'; min: number; max: number };

export function checkLength(value: string, mode: Mode): LengthResult {
  const min = mode === 'mfa' ? MIN_LENGTH_MFA : MIN_LENGTH_NO_MFA;
  if (value.length < min) return { ok: false, code: 'too-short', min, max: MAX_LENGTH };
  if (value.length > MAX_LENGTH) return { ok: false, code: 'too-long', min, max: MAX_LENGTH };
  return { ok: true };
}

export async function sha1Prefix(value: string): Promise<string> {
  // Pwned Passwords API requires the first 5 hex chars of SHA-1, uppercase.
  const enc = new TextEncoder().encode(value);
  const buf = await crypto.subtle.digest('SHA-1', enc);
  const hex = Array.from(new Uint8Array(buf))
    .map((b) => b.toString(16).padStart(2, '0'))
    .join('');
  return hex.slice(0, 5).toUpperCase();
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test tests/unit/password.test.ts
```

Expected: PASS — eight tests green.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/utils/password.ts design-system/tests/unit/password.test.ts
git commit -m "feat(design-system): add password length and SHA-1 prefix helpers"
```

---

## Task 7: Shared types

**Files:**
- Create: `design-system/src/types.ts`

- [ ] **Step 1: Write `src/types.ts`**

Write to `design-system/src/types.ts`:

```ts
export type IconName =
  | 'logo'
  | 'bluesky'
  | 'chevron-up'
  | 'chevron-down'
  | 'chevron-left'
  | 'chevron-right'
  | 'grid-view'
  | 'list-view'
  | 'eye'
  | 'share'
  | 'heart'
  | 'bell'
  | 'x'
  | 'user'
  | 'reply'
  | 'repost'
  | 'like';

export type ButtonVariant =
  | 'primary'
  | 'secondary'
  | 'scrollTop'
  | 'calendarNav'
  | 'form'
  | 'avatar'
  | 'quit';

export type ErrorMessage = {
  key: string;
  vars?: Record<string, string | number>;
};

export type Direction = 'up' | 'down' | 'left' | 'right';
```

- [ ] **Step 2: Verify it type-checks**

```bash
pnpm typecheck
```

Expected: zero errors.

- [ ] **Step 3: Commit**

```bash
git add design-system/src/types.ts
git commit -m "feat(design-system): declare shared types (IconName, ButtonVariant, ErrorMessage)"
```

---

## Task 8: Author conventions document

**Files:**
- Create: `design-system/src/components/CONVENTIONS.md`

- [ ] **Step 1: Write `CONVENTIONS.md`**

Write to `design-system/src/components/CONVENTIONS.md`:

```markdown
# Mitosis source conventions

Rules for `.lite.tsx` components in this directory. These exist because Mitosis transpiles the same source to React, Vue, Svelte, Solid, Qwik, Preact, Lit, Stencil, and Alpine — patterns that work in one target may not work in others.

## Forbidden

- `class` syntax for components. Use functions.
- Synchronous mutation of refs during render — Solid and Qwik break.
- React-specific event-system shortcuts (`event.persist()`, synthetic-event pooling assumptions).
- Direct DOM access during render. Use `onMount` / `onUpdate` lifecycle hooks.
- Default exports. Use named exports with the same name as the file.

## Required

- `useStore({ ... })` for component state. The compiler maps it to each target's idiomatic state primitive.
- Strings come from `t(key)` (default impl in `src/utils/i18n.ts`). Atoms accept `labelKey` (preferred) or `label` (literal fallback). When both are supplied, `labelKey` wins.
- Numbers and dates go through `formatCount` / `formatDate` from `src/utils/intl.ts`.
- Class names use the component-scoped CSS variable convention: `var(--button-bg-primary)`, not raw colours.
- Prop types declared inline above the component, not in a separate file.

## Event handlers

Keep handlers minimal. Mitosis tolerates `onClick={() => props.onClick?.()}` cleanly across targets. Do not destructure `event.target.value` from a synthetic event in a way that assumes React's pooling.

## Slots / children

Mitosis maps `props.children` correctly across all targets. For named slots (e.g. `Checkbox.labelChildren`), declare them as `?: any` and render with `{props.labelChildren}`.

## Styling

Inline `<style>` blocks at the bottom of the component. Reference component-layer CSS variables. Do not import a separate `.css` file — Mitosis can not transport that across targets reliably.

## Testing

Tests live in `tests/unit/<ComponentName>.test.ts`. They import from the **generated** target output (e.g. `output/vue/src/Button.vue` and `output/react/src/Button.tsx`), not from `.lite.tsx` directly — Mitosis source is not a runnable component.
```

- [ ] **Step 2: Commit**

```bash
git add design-system/src/components/CONVENTIONS.md
git commit -m "docs(design-system): document Mitosis authoring conventions"
```

---

## Task 9: Logo atom

**Files:**
- Create: `design-system/src/icons/logo.svg` (copy from `assets/logo.svg`)
- Create: `design-system/src/components/Logo.lite.tsx`
- Create: `design-system/tests/unit/Logo.test.ts`

- [ ] **Step 1: Copy the logo SVG into the package**

```bash
cp ./design-system/assets/logo.svg ./design-system/src/icons/logo.svg
```

- [ ] **Step 2: Write the failing test (against generated Vue and React output)**

Write to `design-system/tests/unit/Logo.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('Logo (post-Mitosis emit)', () => {
  it('emits a Vue component', () => {
    const path = join(root, 'output/vue/src/components/Logo.vue');
    expect(existsSync(path)).toBe(true);
    const src = readFileSync(path, 'utf8');
    expect(src).toContain('<template>');
    expect(src).toMatch(/Revue de presse/i); // alt text via t('logo.alt')
  });

  it('emits a React component', () => {
    const path = join(root, 'output/react/src/components/Logo.tsx');
    expect(existsSync(path)).toBe(true);
    const src = readFileSync(path, 'utf8');
    expect(src).toMatch(/export\s+(default\s+)?(function|const)\s+Logo/);
  });
});
```

- [ ] **Step 3: Run the test to verify it fails**

```bash
pnpm test tests/unit/Logo.test.ts
```

Expected: FAIL — `output/` does not contain Logo files yet.

- [ ] **Step 4: Write `src/components/Logo.lite.tsx`**

Write to `design-system/src/components/Logo.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type LogoProps = {
  size?: 'sm' | 'md' | 'lg';
  showWordmark?: boolean;
  altKey?: string;
};

export default function Logo(props: LogoProps) {
  const dim = props.size === 'sm' ? 32 : props.size === 'lg' ? 64 : 48;

  return (
    <span class="rdp-logo" data-size={props.size ?? 'md'}>
      <svg
        width={dim}
        height={dim}
        viewBox="0 0 58 58"
        role="img"
        aria-label={t(props.altKey ?? 'logo.alt')}
      >
        <circle cx="29" cy="29" r="29" fill="var(--color-brand)" />
        <g transform="translate(6.349 13.196)">
          {/* Path data shipped via assets/logo.svg — see src/icons/logo.svg for source. */}
          <path
            d="M44.485 41.775l-.4-.392-.767-.758-.845.835-.319.315 1.164 2.083z"
            transform="translate(-24.524 -28.899)"
            fill="var(--color-white)"
          />
        </g>
      </svg>
      {props.showWordmark && (
        <span class="rdp-logo__wordmark">Revue de presse</span>
      )}
      <style>{`
        .rdp-logo {
          display: inline-flex;
          align-items: center;
          gap: var(--separation-1);
        }
        .rdp-logo__wordmark {
          font-family: 'Signika', sans-serif;
          font-size: 24px;
          color: var(--color-brand);
        }
      `}</style>
    </span>
  );
}
```

(The simplified `<path>` is for compile-test purposes. The full path data lives in `src/icons/logo.svg`. A future task can promote the full path data into the component when an SVG-as-component build step lands; for M1, the simplified mark is adequate to verify the pipeline.)

- [ ] **Step 5: Run the Mitosis build**

```bash
pnpm build:mitosis
```

Expected: stdout reports two emit operations (`Logo.vue`, `Logo.tsx`). No errors.

- [ ] **Step 6: Run the test to verify it passes**

```bash
pnpm test tests/unit/Logo.test.ts
```

Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add design-system/src/icons/logo.svg design-system/src/components/Logo.lite.tsx design-system/tests/unit/Logo.test.ts design-system/output/react/src/components/Logo.tsx design-system/output/vue/src/components/Logo.vue
git commit -m "feat(design-system): add Logo atom"
```

---

## Task 10: Icon atom + sprite

**Files:**
- Create: `design-system/src/icons/sprite.svg`
- Create: `design-system/src/components/Icon.lite.tsx`
- Create: `design-system/tests/unit/Icon.test.ts`

- [ ] **Step 1: Write the sprite SVG**

The Icon atom uses `<symbol>` references inside one sprite. M1 ships a minimal subset: `chevron-up`, `chevron-down`, `chevron-left`, `chevron-right`, `eye`, `share`, `heart`, `bell`, `x`, `user`. Glyphs come from Lucide (license: ISC).

Write to `design-system/src/icons/sprite.svg`. Each `<symbol>` has the same 24×24 viewBox and uses `currentColor` so consumers can recolor via CSS.

```xml
<svg xmlns="http://www.w3.org/2000/svg" style="display:none">
  <symbol id="chevron-up" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="18 15 12 9 6 15"/>
  </symbol>
  <symbol id="chevron-down" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="6 9 12 15 18 9"/>
  </symbol>
  <symbol id="chevron-left" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="15 18 9 12 15 6"/>
  </symbol>
  <symbol id="chevron-right" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="9 18 15 12 9 6"/>
  </symbol>
  <symbol id="eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7z"/>
    <circle cx="12" cy="12" r="3"/>
  </symbol>
  <symbol id="share" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/>
    <polyline points="16 6 12 2 8 6"/>
    <line x1="12" y1="2" x2="12" y2="15"/>
  </symbol>
  <symbol id="heart" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
  </symbol>
  <symbol id="bell" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
    <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
  </symbol>
  <symbol id="x" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <line x1="18" y1="6" x2="6" y2="18"/>
    <line x1="6" y1="6" x2="18" y2="18"/>
  </symbol>
  <symbol id="user" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
    <circle cx="12" cy="7" r="4"/>
  </symbol>
</svg>
```

- [ ] **Step 2: Write the failing test**

Write to `design-system/tests/unit/Icon.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { readFileSync, existsSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('Icon sprite', () => {
  it('contains every M1 glyph', () => {
    const path = join(root, 'src/icons/sprite.svg');
    const svg = readFileSync(path, 'utf8');
    for (const id of [
      'chevron-up', 'chevron-down', 'chevron-left', 'chevron-right',
      'eye', 'share', 'heart', 'bell', 'x', 'user',
    ]) {
      expect(svg).toContain(`id="${id}"`);
    }
  });
});

describe('Icon (post-Mitosis emit)', () => {
  it('emits a Vue component that uses <use href>', () => {
    const path = join(root, 'output/vue/src/components/Icon.vue');
    expect(existsSync(path)).toBe(true);
    const src = readFileSync(path, 'utf8');
    expect(src).toMatch(/<use[^>]*href/);
  });

  it('emits a React component that uses <use href>', () => {
    const path = join(root, 'output/react/src/components/Icon.tsx');
    expect(existsSync(path)).toBe(true);
    const src = readFileSync(path, 'utf8');
    expect(src).toMatch(/<use[^>]*href/);
  });
});
```

- [ ] **Step 3: Run the test (sprite passes, components fail)**

```bash
pnpm test tests/unit/Icon.test.ts
```

Expected: 1 PASS (sprite has IDs), 2 FAIL (components don't exist).

- [ ] **Step 4: Write `src/components/Icon.lite.tsx`**

Write to `design-system/src/components/Icon.lite.tsx`:

```tsx
import type { IconName } from '../types';

type IconProps = {
  name: IconName;
  size?: 24 | 32;
  ariaLabel?: string;
  decorative?: boolean;
};

export default function Icon(props: IconProps) {
  const dim = props.size ?? 24;
  const isDecorative = props.decorative ?? !props.ariaLabel;

  return (
    <svg
      class="rdp-icon"
      width={dim}
      height={dim}
      viewBox="0 0 24 24"
      aria-hidden={isDecorative ? 'true' : undefined}
      aria-label={isDecorative ? undefined : props.ariaLabel}
      role={isDecorative ? undefined : 'img'}
    >
      <use href={`#${props.name}`} />
      <style>{`
        .rdp-icon { display: inline-block; vertical-align: middle; color: currentColor; }
      `}</style>
    </svg>
  );
}
```

The component references `<use href="#name">`. Consumers must include `sprite.svg` once at the top of the page (the demo app does this).

- [ ] **Step 5: Run the Mitosis build**

```bash
pnpm build:mitosis
```

Expected: emit succeeds.

- [ ] **Step 6: Run the test to verify all three pass**

```bash
pnpm test tests/unit/Icon.test.ts
```

Expected: PASS — all three.

- [ ] **Step 7: Commit**

```bash
git add design-system/src/icons/sprite.svg design-system/src/components/Icon.lite.tsx design-system/tests/unit/Icon.test.ts design-system/output
git commit -m "feat(design-system): add Icon atom and Lucide-derived sprite"
```

---

## Task 11: Button atom

**Files:**
- Create: `design-system/src/components/Button.lite.tsx`
- Create: `design-system/tests/unit/Button.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/Button.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('Button (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/Button.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      // Variant prop is wired through (string union shows up in TS source)
      expect(src).toMatch(/variant/);
      // Calls onClick
      expect(src).toMatch(/onClick/);
      // Disabled state is honoured
      expect(src).toMatch(/disabled/);
    });
  }
});
```

- [ ] **Step 2: Run the test to verify it fails**

```bash
pnpm test tests/unit/Button.test.ts
```

Expected: FAIL — no Button output.

- [ ] **Step 3: Write `src/components/Button.lite.tsx`**

Write to `design-system/src/components/Button.lite.tsx`:

```tsx
import { t } from '../utils/i18n';
import type { ButtonVariant, IconName } from '../types';

type ButtonProps = {
  variant: ButtonVariant;
  labelKey?: string;
  label?: string;
  icon?: IconName;
  iconAfter?: IconName;
  disabled?: boolean;
  loading?: boolean;
  ariaLabel?: string;
  onClick?: () => void;
};

export default function Button(props: ButtonProps) {
  const text = props.labelKey ? t(props.labelKey) : (props.label ?? '');
  const isCircular = props.variant === 'scrollTop' || props.variant === 'avatar';

  return (
    <button
      type="button"
      class={`rdp-button rdp-button--${props.variant}`}
      data-loading={props.loading ? 'true' : undefined}
      disabled={props.disabled || props.loading}
      aria-label={props.ariaLabel}
      onClick={(event) => props.onClick?.()}
    >
      {props.icon && <span class="rdp-button__icon">{/* Icon name: {props.icon} */}</span>}
      {!isCircular && text && <span class="rdp-button__label">{text}</span>}
      {props.iconAfter && <span class="rdp-button__icon-after">{/* Icon name: {props.iconAfter} */}</span>}
      <style>{`
        .rdp-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: var(--separation-1);
          border: none;
          cursor: pointer;
          font-family: 'Roboto', sans-serif;
          font-size: var(--font-size-content);
          padding: var(--separation-1) var(--separation-2);
          border-radius: var(--radius-default);
          background: var(--button-bg-primary);
          color: var(--button-fg-primary);
          transition: background 120ms;
        }
        .rdp-button:hover { background: var(--button-bg-primary-hover); }
        .rdp-button:disabled {
          background: var(--button-bg-disabled);
          cursor: not-allowed;
        }
        .rdp-button--secondary {
          background: var(--button-bg-secondary);
          color: var(--color-brand-active);
          border: 1px solid var(--button-border-secondary);
        }
        .rdp-button--scrollTop,
        .rdp-button--avatar {
          width: 48px;
          height: 48px;
          padding: 0;
          border-radius: 50%;
        }
        .rdp-button--quit {
          background: transparent;
          color: var(--color-content-text);
          padding: 0;
        }
        .rdp-button--calendarNav {
          padding: 0;
          width: 32px;
          height: 32px;
          border-radius: 50%;
        }
        .rdp-button--form {
          width: 100%;
          padding: var(--separation-2);
        }
      `}</style>
    </button>
  );
}
```

- [ ] **Step 4: Run the Mitosis build**

```bash
pnpm build:mitosis
```

Expected: emits Button to `output/vue` and `output/react`.

- [ ] **Step 5: Run the test to verify it passes**

```bash
pnpm test tests/unit/Button.test.ts
```

Expected: PASS — both targets.

- [ ] **Step 6: Commit**

```bash
git add design-system/src/components/Button.lite.tsx design-system/tests/unit/Button.test.ts design-system/output
git commit -m "feat(design-system): add Button atom with seven variants"
```

---

## Task 12: Link atom

**Files:**
- Create: `design-system/src/components/Link.lite.tsx`
- Create: `design-system/tests/unit/Link.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/Link.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('Link (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/Link.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/href/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/Link.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/Link.lite.tsx`**

Write to `design-system/src/components/Link.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type LinkProps = {
  href: string;
  labelKey?: string;
  label?: string;
  external?: boolean;
  variant?: 'underline' | 'inline' | 'plain';
};

export default function Link(props: LinkProps) {
  const text = props.labelKey ? t(props.labelKey) : (props.label ?? '');
  const variant = props.variant ?? 'underline';

  return (
    <a
      class={`rdp-link rdp-link--${variant}`}
      href={props.href}
      target={props.external ? '_blank' : undefined}
      rel={props.external ? 'noopener noreferrer' : undefined}
    >
      {text}
      <style>{`
        .rdp-link {
          color: var(--color-brand-active);
          text-decoration: none;
          font-family: 'Roboto', sans-serif;
        }
        .rdp-link--underline { text-decoration: underline; }
        .rdp-link--plain { color: inherit; }
        .rdp-link:hover { color: var(--color-brand); }
      `}</style>
    </a>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/Link.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/Link.lite.tsx design-system/tests/unit/Link.test.ts design-system/output
git commit -m "feat(design-system): add Link atom"
```

---

## Task 13: Checkbox atom (with `labelChildren` slot)

**Files:**
- Create: `design-system/src/components/Checkbox.lite.tsx`
- Create: `design-system/tests/unit/Checkbox.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/Checkbox.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('Checkbox (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component supporting labelChildren slot`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/Checkbox.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      // Renders the labelChildren slot
      expect(src).toMatch(/labelChildren/);
      // input type=checkbox
      expect(src).toMatch(/type=["']?checkbox/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/Checkbox.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/Checkbox.lite.tsx`**

Write to `design-system/src/components/Checkbox.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type CheckboxProps = {
  name: string;
  checked?: boolean;
  onChange?: (checked: boolean) => void;
  labelKey?: string;
  label?: string;
  labelChildren?: any; // slot for inline link rendering (ToS, etc.)
  disabled?: boolean;
};

export default function Checkbox(props: CheckboxProps) {
  const text = props.labelKey ? t(props.labelKey) : (props.label ?? '');

  return (
    <label class="rdp-checkbox">
      <input
        type="checkbox"
        name={props.name}
        checked={props.checked ?? false}
        disabled={props.disabled}
        onChange={(event) => props.onChange?.((event.target as HTMLInputElement).checked)}
      />
      <span class="rdp-checkbox__label">
        {text}
        {props.labelChildren}
      </span>
      <style>{`
        .rdp-checkbox {
          display: inline-flex;
          align-items: center;
          gap: var(--separation-1);
          font-family: 'Roboto', sans-serif;
          font-size: var(--font-size-content);
          color: var(--color-content-text);
          cursor: pointer;
        }
        .rdp-checkbox__label { display: inline-flex; gap: 4px; align-items: baseline; }
        .rdp-checkbox input { accent-color: var(--color-brand-active); }
        .rdp-checkbox input:disabled + .rdp-checkbox__label { opacity: 0.5; }
      `}</style>
    </label>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/Checkbox.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/Checkbox.lite.tsx design-system/tests/unit/Checkbox.test.ts design-system/output
git commit -m "feat(design-system): add Checkbox atom with labelChildren slot"
```

---

## Task 14: TextField atom

**Files:**
- Create: `design-system/src/components/TextField.lite.tsx`
- Create: `design-system/tests/unit/TextField.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/TextField.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('TextField (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component with error styling`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/TextField.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/error/);
      expect(src).toMatch(/value/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/TextField.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/TextField.lite.tsx`**

Write to `design-system/src/components/TextField.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type TextFieldProps = {
  name: string;
  type?: 'text' | 'email' | 'tel' | 'url';
  value: string;
  onChange?: (value: string) => void;
  labelKey?: string;
  label?: string;
  placeholder?: string;
  autocomplete?: string;
  error?: boolean;
  required?: boolean;
};

export default function TextField(props: TextFieldProps) {
  const labelText = props.labelKey ? t(props.labelKey) : (props.label ?? '');

  return (
    <div class="rdp-textfield" data-error={props.error ? 'true' : undefined}>
      <label class="rdp-textfield__label" for={`field-${props.name}`}>
        {labelText}
      </label>
      <input
        id={`field-${props.name}`}
        type={props.type ?? 'text'}
        name={props.name}
        value={props.value}
        placeholder={props.placeholder}
        autocomplete={props.autocomplete}
        required={props.required}
        onInput={(event) => props.onChange?.((event.target as HTMLInputElement).value)}
      />
      <style>{`
        .rdp-textfield { display: flex; flex-direction: column; gap: 4px; font-family: 'Roboto', sans-serif; }
        .rdp-textfield__label {
          font-size: var(--font-size-publication-date);
          color: var(--color-content-text);
          padding-left: var(--separation-1);
        }
        .rdp-textfield input {
          padding: var(--separation-1) var(--separation-2);
          border: 1px solid var(--input-border-default);
          background: var(--input-bg-default);
          color: var(--input-fg-default);
          border-radius: var(--radius-default);
          font-size: var(--font-size-content);
        }
        .rdp-textfield input:focus {
          outline: none;
          border-color: var(--color-brand-active);
        }
        .rdp-textfield[data-error="true"] input { border-color: var(--input-border-error); }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/TextField.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/TextField.lite.tsx design-system/tests/unit/TextField.test.ts design-system/output
git commit -m "feat(design-system): add TextField atom with error styling"
```

---

## Task 15: EmailField atom

**Files:**
- Create: `design-system/src/components/EmailField.lite.tsx`
- Create: `design-system/tests/unit/EmailField.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/EmailField.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('EmailField (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component using type=email`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/EmailField.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/type=["']?email/);
      expect(src).toMatch(/autocomplete=["']?email/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/EmailField.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/EmailField.lite.tsx`**

Write to `design-system/src/components/EmailField.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type EmailFieldProps = {
  name: string;
  value: string;
  onChange?: (value: string) => void;
  labelKey?: string;
  error?: boolean;
  required?: boolean;
};

export default function EmailField(props: EmailFieldProps) {
  const labelText = t(props.labelKey ?? 'auth.email-label');

  return (
    <div class="rdp-textfield" data-error={props.error ? 'true' : undefined}>
      <label class="rdp-textfield__label" for={`field-${props.name}`}>
        {labelText}
      </label>
      <input
        id={`field-${props.name}`}
        type="email"
        autocomplete="email"
        name={props.name}
        value={props.value}
        required={props.required}
        onInput={(event) => props.onChange?.((event.target as HTMLInputElement).value)}
      />
      <style>{`
        .rdp-textfield { display: flex; flex-direction: column; gap: 4px; font-family: 'Roboto', sans-serif; }
        .rdp-textfield__label {
          font-size: var(--font-size-publication-date);
          color: var(--color-content-text);
          padding-left: var(--separation-1);
        }
        .rdp-textfield input {
          padding: var(--separation-1) var(--separation-2);
          border: 1px solid var(--input-border-default);
          background: var(--input-bg-default);
          color: var(--input-fg-default);
          border-radius: var(--radius-default);
          font-size: var(--font-size-content);
        }
        .rdp-textfield[data-error="true"] input { border-color: var(--input-border-error); }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/EmailField.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/EmailField.lite.tsx design-system/tests/unit/EmailField.test.ts design-system/output
git commit -m "feat(design-system): add EmailField atom"
```

---

## Task 16: PasswordField atom (M1 subset — no zxcvbn or Pwned)

**Files:**
- Create: `design-system/src/components/PasswordField.lite.tsx`
- Create: `design-system/tests/unit/PasswordField.test.ts`

> M1 ships only length validation. Strength meter (zxcvbn-ts) and breach checking (Pwned Passwords API) land in M3.

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/PasswordField.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('PasswordField (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component with mode prop`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/PasswordField.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/mode/);
      expect(src).toMatch(/type=["']?password/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/PasswordField.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/PasswordField.lite.tsx`**

Write to `design-system/src/components/PasswordField.lite.tsx`:

```tsx
import { t } from '../utils/i18n';
import { MIN_LENGTH_NO_MFA } from '../utils/password';

type PasswordFieldProps = {
  name: string;
  value: string;
  onChange?: (value: string) => void;
  mode: 'login' | 'new';
  labelKey?: string;
  error?: boolean;
  showHelper?: boolean;
};

export default function PasswordField(props: PasswordFieldProps) {
  const labelText = t(props.labelKey ?? 'auth.password-label');
  const helperText = t('auth.password.helper-text', { min: MIN_LENGTH_NO_MFA });
  const helperVisible = (props.showHelper ?? props.mode === 'new');

  return (
    <div class="rdp-textfield" data-error={props.error ? 'true' : undefined}>
      <label class="rdp-textfield__label" for={`field-${props.name}`}>
        {labelText}
      </label>
      <input
        id={`field-${props.name}`}
        type="password"
        autocomplete={props.mode === 'new' ? 'new-password' : 'current-password'}
        name={props.name}
        value={props.value}
        onInput={(event) => props.onChange?.((event.target as HTMLInputElement).value)}
      />
      {helperVisible && (
        <p class="rdp-textfield__helper">{helperText}</p>
      )}
      <style>{`
        .rdp-textfield { display: flex; flex-direction: column; gap: 4px; font-family: 'Roboto', sans-serif; }
        .rdp-textfield__label {
          font-size: var(--font-size-publication-date);
          color: var(--color-content-text);
          padding-left: var(--separation-1);
        }
        .rdp-textfield__helper {
          font-size: var(--font-size-publication-date);
          color: var(--color-light-grey);
          padding-left: var(--separation-1);
          margin: 0;
        }
        .rdp-textfield input {
          padding: var(--separation-1) var(--separation-2);
          border: 1px solid var(--input-border-default);
          background: var(--input-bg-default);
          color: var(--input-fg-default);
          border-radius: var(--radius-default);
          font-size: var(--font-size-content);
        }
        .rdp-textfield[data-error="true"] input { border-color: var(--input-border-error); }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/PasswordField.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/PasswordField.lite.tsx design-system/tests/unit/PasswordField.test.ts design-system/output
git commit -m "feat(design-system): add PasswordField atom (length-only validation in M1)"
```

---

## Task 17: FieldError atom

**Files:**
- Create: `design-system/src/components/FieldError.lite.tsx`
- Create: `design-system/tests/unit/FieldError.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/FieldError.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('FieldError (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/FieldError.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/role=["']?alert/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/FieldError.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/FieldError.lite.tsx`**

Write to `design-system/src/components/FieldError.lite.tsx`:

```tsx
import { t } from '../utils/i18n';

type FieldErrorProps = {
  messageKey?: string;
  message?: string;
  vars?: Record<string, string | number>;
};

export default function FieldError(props: FieldErrorProps) {
  const text = props.messageKey ? t(props.messageKey, props.vars) : (props.message ?? '');
  if (!text) return null;
  return (
    <p class="rdp-field-error" role="alert">
      {text}
      <style>{`
        .rdp-field-error {
          font-size: var(--font-size-publication-date);
          color: var(--form-error-fg);
          margin: 0;
          padding-left: var(--separation-1);
          font-family: 'Roboto', sans-serif;
        }
      `}</style>
    </p>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/FieldError.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/FieldError.lite.tsx design-system/tests/unit/FieldError.test.ts design-system/output
git commit -m "feat(design-system): add FieldError atom"
```

---

## Task 18: FormError atom (multi-error stacking)

**Files:**
- Create: `design-system/src/components/FormError.lite.tsx`
- Create: `design-system/tests/unit/FormError.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/FormError.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('FormError (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component that loops over errors`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/FormError.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/errors/);
      expect(src).toMatch(/role=["']?alert/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/FormError.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/FormError.lite.tsx`**

Write to `design-system/src/components/FormError.lite.tsx`:

```tsx
import { t } from '../utils/i18n';
import type { ErrorMessage } from '../types';

type FormErrorProps = {
  errors: ErrorMessage[];
};

export default function FormError(props: FormErrorProps) {
  const items = (props.errors ?? []).map((e) => ({ key: e.key, text: t(e.key, e.vars) }));
  if (items.length === 0) return null;

  return (
    <div class="rdp-form-error" role="alert" aria-live="polite">
      {items.map((item, idx) => (
        <p class="rdp-form-error__item" key={item.key}>
          {items.length > 1 ? `- ${item.text}` : item.text}
        </p>
      ))}
      <style>{`
        .rdp-form-error {
          color: var(--form-error-fg);
          font-size: var(--font-size-publication-date);
          font-family: 'Roboto', sans-serif;
          margin-top: var(--separation-1);
        }
        .rdp-form-error__item { margin: 0; }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/FormError.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/FormError.lite.tsx design-system/tests/unit/FormError.test.ts design-system/output
git commit -m "feat(design-system): add FormError atom for multi-error stacking"
```

---

## Task 19: MediaPlaceholder atom

**Files:**
- Create: `design-system/src/components/MediaPlaceholder.lite.tsx`
- Create: `design-system/tests/unit/MediaPlaceholder.test.ts`

- [ ] **Step 1: Write the failing test**

Write to `design-system/tests/unit/MediaPlaceholder.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const root = join(__dirname, '../..');

describe('MediaPlaceholder (post-Mitosis emit)', () => {
  for (const target of ['vue', 'react']) {
    it(`emits a ${target} component`, () => {
      const ext = target === 'vue' ? 'vue' : 'tsx';
      const path = join(root, `output/${target}/src/components/MediaPlaceholder.${ext}`);
      expect(existsSync(path)).toBe(true);
      const src = readFileSync(path, 'utf8');
      expect(src).toMatch(/aspectRatio|aspect-ratio/);
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test tests/unit/MediaPlaceholder.test.ts
```

Expected: FAIL.

- [ ] **Step 3: Write `src/components/MediaPlaceholder.lite.tsx`**

Write to `design-system/src/components/MediaPlaceholder.lite.tsx`:

```tsx
type MediaPlaceholderProps = {
  width?: number;
  height?: number;
  aspectRatio?: string; // e.g. "16 / 9"
};

export default function MediaPlaceholder(props: MediaPlaceholderProps) {
  return (
    <div
      class="rdp-media-placeholder"
      style={`width:${props.width ? `${props.width}px` : '100%'};${
        props.height ? `height:${props.height}px;` : ''
      }${props.aspectRatio ? `aspect-ratio:${props.aspectRatio};` : ''}`}
      aria-hidden="true"
    >
      <svg viewBox="0 0 100 60" preserveAspectRatio="none">
        <line x1="0" y1="0" x2="100" y2="60" stroke="var(--color-light-grey)" strokeWidth="1" />
        <line x1="100" y1="0" x2="0" y2="60" stroke="var(--color-light-grey)" strokeWidth="1" />
      </svg>
      <style>{`
        .rdp-media-placeholder {
          display: block;
          background: #b0c4d4;
          border-radius: var(--radius-default);
          overflow: hidden;
          max-width: 270px;
        }
        .rdp-media-placeholder svg { width: 100%; height: 100%; display: block; }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 4: Build and verify**

```bash
pnpm build:mitosis
pnpm test tests/unit/MediaPlaceholder.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add design-system/src/components/MediaPlaceholder.lite.tsx design-system/tests/unit/MediaPlaceholder.test.ts design-system/output
git commit -m "feat(design-system): add MediaPlaceholder atom"
```

---

## Task 20: Components barrel export

**Files:**
- Create: `design-system/src/components/index.ts`

- [ ] **Step 1: Write `src/components/index.ts`**

Write to `design-system/src/components/index.ts`:

```ts
// Re-exports for the .lite.tsx authoring layer. Consumers import from the
// per-target output package, not from this file directly.

export { default as Button } from './Button.lite';
export { default as Checkbox } from './Checkbox.lite';
export { default as EmailField } from './EmailField.lite';
export { default as FieldError } from './FieldError.lite';
export { default as FormError } from './FormError.lite';
export { default as Icon } from './Icon.lite';
export { default as Link } from './Link.lite';
export { default as Logo } from './Logo.lite';
export { default as MediaPlaceholder } from './MediaPlaceholder.lite';
export { default as PasswordField } from './PasswordField.lite';
export { default as TextField } from './TextField.lite';
```

- [ ] **Step 2: Type-check**

```bash
pnpm typecheck
```

Expected: zero errors.

- [ ] **Step 3: Commit**

```bash
git add design-system/src/components/index.ts
git commit -m "feat(design-system): add components barrel export"
```

---

## Task 21: Demo Vite + Vue app

**Files:**
- Create: `design-system/apps/demo/package.json`
- Create: `design-system/apps/demo/tsconfig.json`
- Create: `design-system/apps/demo/vite.config.ts`
- Create: `design-system/apps/demo/index.html`
- Create: `design-system/apps/demo/src/main.ts`
- Create: `design-system/apps/demo/src/App.vue`

- [ ] **Step 1: Write `apps/demo/package.json`**

Write to `design-system/apps/demo/package.json`:

```json
{
  "name": "@revue-de-presse/design-system-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vue-tsc": "^2.0.0"
  }
}
```

- [ ] **Step 2: Write `apps/demo/tsconfig.json`**

Write to `design-system/apps/demo/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "jsx": "preserve",
    "types": ["vite/client"]
  },
  "include": ["src/**/*", "../../output/vue/src/**/*", "../../src/tokens/tokens.css"]
}
```

- [ ] **Step 3: Write `apps/demo/vite.config.ts`**

Write to `design-system/apps/demo/vite.config.ts`:

```ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { fileURLToPath, URL } from 'node:url';

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@design-system': fileURLToPath(new URL('../../output/vue/src', import.meta.url)),
      '@tokens': fileURLToPath(new URL('../../src/tokens', import.meta.url)),
      '@icons': fileURLToPath(new URL('../../src/icons', import.meta.url)),
    },
  },
});
```

- [ ] **Step 4: Write `apps/demo/index.html`**

Write to `design-system/apps/demo/index.html`:

```html
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Revue de presse — Design system M1 demo</title>
  <link rel="stylesheet" href="/src/tokens.css" />
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

- [ ] **Step 5: Write `apps/demo/src/main.ts`**

Write to `design-system/apps/demo/src/main.ts`:

```ts
import { createApp } from 'vue';
import App from './App.vue';
import '@tokens/tokens.css';

createApp(App).mount('#app');
```

- [ ] **Step 6: Write `apps/demo/src/App.vue`**

Write to `design-system/apps/demo/src/App.vue`:

```vue
<script setup lang="ts">
import { ref } from 'vue';
import Logo from '@design-system/components/Logo.vue';
import Icon from '@design-system/components/Icon.vue';
import Button from '@design-system/components/Button.vue';
import Link from '@design-system/components/Link.vue';
import Checkbox from '@design-system/components/Checkbox.vue';
import TextField from '@design-system/components/TextField.vue';
import EmailField from '@design-system/components/EmailField.vue';
import PasswordField from '@design-system/components/PasswordField.vue';
import FieldError from '@design-system/components/FieldError.vue';
import FormError from '@design-system/components/FormError.vue';
import MediaPlaceholder from '@design-system/components/MediaPlaceholder.vue';
import spriteUrl from '@icons/sprite.svg?url';

const email = ref('');
const password = ref('');
const tos = ref(false);
const showError = ref(false);
</script>

<template>
  <main>
    <!-- Mount the icon sprite once at the top of the document -->
    <object :data="spriteUrl" type="image/svg+xml" style="display:none" />

    <header>
      <Logo show-wordmark />
    </header>

    <section>
      <h2>Buttons</h2>
      <Button variant="primary" label="Connexion" />
      <Button variant="secondary" label="Avril 2021" />
      <Button variant="form" label="S'inscrire" />
      <Button variant="quit" label="Quitter" icon-after="x" />
      <Button variant="scrollTop" icon="chevron-up" aria-label="Remonter" />
    </section>

    <section>
      <h2>Form</h2>
      <EmailField name="email" :value="email" @change="email = $event" />
      <PasswordField name="password" :value="password" mode="login" @change="password = $event" />
      <Checkbox name="tos" :checked="tos" @change="tos = $event" label-key="auth.tos.checkbox-label" />
      <FormError v-if="showError" :errors="[
        { key: 'errors.password.too-short', vars: { min: 15 } },
        { key: 'errors.tos.not-accepted' }
      ]" />
      <Button variant="form" label="Tester l'erreur" @click="showError = true" />
    </section>

    <section>
      <h2>Misc</h2>
      <Link href="https://revue-de-presse.org" external label="revue-de-presse.org" />
      <Icon name="heart" />
      <Icon name="share" />
      <MediaPlaceholder :width="270" :height="160" />
    </section>
  </main>
</template>

<style>
body { margin: 0; background: var(--color-taupe-grey); font-family: 'Roboto', sans-serif; }
main { padding: var(--separation-3); display: grid; gap: var(--separation-3); }
section { display: flex; flex-direction: column; gap: var(--separation-2); }
section > * + * { margin-left: 0; }
h2 { margin: 0; font-family: 'Signika', sans-serif; color: var(--color-content-text); }
</style>
```

- [ ] **Step 7: Install demo deps**

```bash
pnpm install
```

Expected: pnpm picks up the new workspace package and installs `@vitejs/plugin-vue`, `vite`, `vue-tsc`.

- [ ] **Step 8: Boot the demo**

```bash
pnpm demo
```

Expected: Vite dev server boots at `http://localhost:5173`. Open it manually and verify atoms render. Stop with `Ctrl+C`.

If imports fail because Mitosis hasn't been re-run, run `pnpm build:mitosis` first.

- [ ] **Step 9: Commit**

```bash
git add design-system/apps/demo design-system/pnpm-lock.yaml
git commit -m "feat(design-system): add demo Vite/Vue app exercising all M1 atoms"
```

---

## Task 22: Histoire stories (one per atom)

**Files:**
- Create: `design-system/stories/histoire.config.ts`
- Create: `design-system/stories/Logo.story.vue`
- Create: `design-system/stories/Button.story.vue`
- Create: `design-system/stories/Checkbox.story.vue`
- Create: `design-system/stories/Link.story.vue`
- Create: `design-system/stories/Icon.story.vue`
- Create: `design-system/stories/TextField.story.vue`
- Create: `design-system/stories/EmailField.story.vue`
- Create: `design-system/stories/PasswordField.story.vue`
- Create: `design-system/stories/FieldError.story.vue`
- Create: `design-system/stories/FormError.story.vue`
- Create: `design-system/stories/MediaPlaceholder.story.vue`

- [ ] **Step 1: Write `stories/histoire.config.ts`**

Write to `design-system/stories/histoire.config.ts`:

```ts
import { defineConfig } from 'histoire';
import { HstVue } from '@histoire/plugin-vue';
import { fileURLToPath, URL } from 'node:url';

export default defineConfig({
  plugins: [HstVue()],
  storyMatch: ['stories/**/*.story.vue'],
  vite: {
    resolve: {
      alias: {
        '@design-system': fileURLToPath(new URL('../output/vue/src', import.meta.url)),
        '@tokens': fileURLToPath(new URL('../src/tokens', import.meta.url)),
      },
    },
  },
  setupFile: undefined,
});
```

- [ ] **Step 2: Write `stories/Logo.story.vue`**

```vue
<script setup lang="ts">
import Logo from '@design-system/components/Logo.vue';
</script>

<template>
  <Story title="Atoms/Logo">
    <Variant title="default"><Logo /></Variant>
    <Variant title="with wordmark"><Logo show-wordmark /></Variant>
    <Variant title="small"><Logo size="sm" show-wordmark /></Variant>
    <Variant title="large"><Logo size="lg" show-wordmark /></Variant>
  </Story>
</template>
```

- [ ] **Step 3: Write `stories/Button.story.vue`**

```vue
<script setup lang="ts">
import Button from '@design-system/components/Button.vue';
</script>

<template>
  <Story title="Atoms/Button">
    <Variant title="primary"><Button variant="primary" label="Ven. 2 avr. 2021" /></Variant>
    <Variant title="secondary"><Button variant="secondary" label="Avril 2021" /></Variant>
    <Variant title="form"><Button variant="form" label="Connexion" /></Variant>
    <Variant title="quit"><Button variant="quit" label="Quitter" icon-after="x" /></Variant>
    <Variant title="scrollTop"><Button variant="scrollTop" icon="chevron-up" aria-label="Remonter" /></Variant>
    <Variant title="avatar"><Button variant="avatar" icon="user" aria-label="Compte" /></Variant>
    <Variant title="calendarNav"><Button variant="calendarNav" icon="chevron-left" aria-label="Précédent" /></Variant>
    <Variant title="disabled"><Button variant="form" label="Connexion" disabled /></Variant>
    <Variant title="loading"><Button variant="form" label="Connexion" loading /></Variant>
  </Story>
</template>
```

- [ ] **Step 4: Write `stories/Checkbox.story.vue`**

```vue
<script setup lang="ts">
import { ref } from 'vue';
import Checkbox from '@design-system/components/Checkbox.vue';
import Link from '@design-system/components/Link.vue';
const checked = ref(false);
</script>

<template>
  <Story title="Atoms/Checkbox">
    <Variant title="default">
      <Checkbox name="example" :checked="checked" @change="checked = $event" label-key="auth.remember-me" />
    </Variant>
    <Variant title="with inline link (ToS)">
      <Checkbox name="tos" :checked="checked" @change="checked = $event" label="J'accepte les ">
        <template #labelChildren>
          <Link href="/tos" label="conditions d'utilisation" variant="inline" />
        </template>
      </Checkbox>
    </Variant>
    <Variant title="disabled">
      <Checkbox name="x" :checked="false" disabled label="Disabled" />
    </Variant>
  </Story>
</template>
```

(Note: depending on how Mitosis emits Vue slots for the `labelChildren` prop, this story may need an adjustment after running `pnpm build:mitosis` and inspecting `output/vue/src/components/Checkbox.vue`. If `labelChildren` is rendered as `{{ labelChildren }}` rather than a slot, pass it as a string prop or update the lite source to use `<slot name="labelChildren" />`. Document the resolution in the commit message.)

- [ ] **Step 5: Write the remaining seven stories (boilerplate)**

For brevity, the remaining stories follow the same shape — one variant per relevant prop. Each is one short file under `stories/`. The engineer writes:

`stories/Link.story.vue`:

```vue
<script setup lang="ts">
import Link from '@design-system/components/Link.vue';
</script>
<template>
  <Story title="Atoms/Link">
    <Variant title="underline"><Link href="#" label="mot de passe oublié ?" /></Variant>
    <Variant title="inline"><Link href="#" label="conditions d'utilisation" variant="inline" /></Variant>
    <Variant title="external"><Link href="https://revue-de-presse.org" external label="revue-de-presse.org" /></Variant>
  </Story>
</template>
```

`stories/Icon.story.vue`:

```vue
<script setup lang="ts">
import Icon from '@design-system/components/Icon.vue';
import spriteUrl from '../src/icons/sprite.svg?url';
</script>
<template>
  <Story title="Atoms/Icon">
    <object :data="spriteUrl" type="image/svg+xml" style="display:none" />
    <Variant title="all glyphs">
      <div style="display:flex;gap:8px;flex-wrap:wrap;">
        <Icon name="chevron-up" /><Icon name="chevron-down" /><Icon name="chevron-left" /><Icon name="chevron-right" />
        <Icon name="eye" /><Icon name="share" /><Icon name="heart" /><Icon name="bell" /><Icon name="x" /><Icon name="user" />
      </div>
    </Variant>
    <Variant title="32px">
      <Icon name="user" :size="32" />
    </Variant>
  </Story>
</template>
```

`stories/TextField.story.vue`:

```vue
<script setup lang="ts">
import { ref } from 'vue';
import TextField from '@design-system/components/TextField.vue';
const value = ref('');
</script>
<template>
  <Story title="Atoms/TextField">
    <Variant title="default"><TextField name="t" :value="value" @change="value = $event" label="email" placeholder="prenom.nom@email.com" /></Variant>
    <Variant title="error"><TextField name="t" value="malformed" error label="email" /></Variant>
  </Story>
</template>
```

`stories/EmailField.story.vue`:

```vue
<script setup lang="ts">
import { ref } from 'vue';
import EmailField from '@design-system/components/EmailField.vue';
const value = ref('');
</script>
<template>
  <Story title="Atoms/EmailField">
    <Variant title="default"><EmailField name="e" :value="value" @change="value = $event" /></Variant>
    <Variant title="error"><EmailField name="e" value="malformed" error /></Variant>
  </Story>
</template>
```

`stories/PasswordField.story.vue`:

```vue
<script setup lang="ts">
import { ref } from 'vue';
import PasswordField from '@design-system/components/PasswordField.vue';
const value = ref('');
</script>
<template>
  <Story title="Atoms/PasswordField">
    <Variant title="login"><PasswordField name="p" :value="value" @change="value = $event" mode="login" /></Variant>
    <Variant title="new (with helper)"><PasswordField name="p" :value="value" @change="value = $event" mode="new" /></Variant>
    <Variant title="error"><PasswordField name="p" value="short" mode="new" error /></Variant>
  </Story>
</template>
```

`stories/FieldError.story.vue`:

```vue
<script setup lang="ts">
import FieldError from '@design-system/components/FieldError.vue';
</script>
<template>
  <Story title="Atoms/FieldError">
    <Variant title="message"><FieldError message-key="errors.email.invalid" /></Variant>
  </Story>
</template>
```

`stories/FormError.story.vue`:

```vue
<script setup lang="ts">
import FormError from '@design-system/components/FormError.vue';
</script>
<template>
  <Story title="Atoms/FormError">
    <Variant title="single">
      <FormError :errors="[{ key: 'errors.auth.invalid-credentials' }]" />
    </Variant>
    <Variant title="multiple">
      <FormError :errors="[
        { key: 'errors.password.too-short', vars: { min: 15 } },
        { key: 'errors.tos.not-accepted' }
      ]" />
    </Variant>
  </Story>
</template>
```

`stories/MediaPlaceholder.story.vue`:

```vue
<script setup lang="ts">
import MediaPlaceholder from '@design-system/components/MediaPlaceholder.vue';
</script>
<template>
  <Story title="Atoms/MediaPlaceholder">
    <Variant title="270x160"><MediaPlaceholder :width="270" :height="160" /></Variant>
    <Variant title="aspect 16/9"><MediaPlaceholder aspect-ratio="16 / 9" /></Variant>
  </Story>
</template>
```

- [ ] **Step 6: Boot Histoire**

```bash
pnpm story
```

Expected: Histoire dev server boots, listing 11 atoms. Open in browser, verify each story renders. Stop with `Ctrl+C`.

- [ ] **Step 7: Commit**

```bash
git add design-system/stories
git commit -m "feat(design-system): add Histoire stories for all M1 atoms"
```

---

## Task 23: GitHub Actions CI

**Files:**
- Create: `design-system/.github/workflows/ci.yml`

- [ ] **Step 1: Write the workflow**

Write to `design-system/.github/workflows/ci.yml`:

```yaml
name: design-system-ci

on:
  push:
    branches: [main]
    paths: ['design-system/**']
  pull_request:
    paths: ['design-system/**']

jobs:
  build-and-test:
    name: build-and-test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: design-system
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: design-system/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: Token sync check
        run: node scripts/sync-tokens.mjs --check
      - name: Mitosis build
        run: pnpm build:mitosis
      - name: Unit tests
        run: pnpm test
      - name: Type check
        run: pnpm typecheck
```

- [ ] **Step 2: Commit**

```bash
git add design-system/.github
git commit -m "ci(design-system): add GitHub Actions workflow for build and test"
```

The workflow runs the same commands a developer does locally: token sync check, Mitosis emit, unit tests, type check.

---

## Task 24: README

**Files:**
- Create: `design-system/README.md`

- [ ] **Step 1: Write the README**

Write to `design-system/README.md`:

````markdown
# @revue-de-presse/design-system

Framework-agnostic design system for [revue-de-presse.org](https://revue-de-presse.org), authored once in [Mitosis](https://github.com/BuilderIO/mitosis) and compiled to React, Vue, Svelte, Solid, Qwik, Preact, Lit, Stencil, and Alpine.

> **M1 status:** foundation tokens + 11 atoms compiled to React and Vue. M2 (molecules + 3 more generators), M3 (organisms + auth with OWASP password rules), M4 (remaining 5 generators), and M5 (publish v1.0.0) are tracked separately.

## Quick start (local dev)

```bash
pnpm install
pnpm build           # tokens parity check + Mitosis emit
pnpm test            # Vitest unit suite
pnpm story           # Histoire stories (port 6006)
pnpm demo            # Vite + Vue showcase app (port 5173)
```

## Layout

- `src/components/*.lite.tsx` — Mitosis source. One component per file. See `src/components/CONVENTIONS.md` for authoring rules.
- `src/tokens/tokens.css` + `tokens.json` — foundation + component-layer tokens, kept in sync by `scripts/sync-tokens.mjs`.
- `src/locales/{fr-FR,en-GB}.json` — i18n dictionaries with strict key parity (enforced by `tests/unit/i18n.test.ts`).
- `src/utils/` — i18n, Intl, password helpers.
- `src/icons/sprite.svg` — Lucide-derived icon sprite (mount once per page).
- `output/<target>/` — Mitosis emit (gitignored). One package per generator target.
- `apps/demo/` — Vite + Vue showcase consuming `output/vue/`.
- `stories/` — Histoire stories.
- `tests/unit/` — Vitest unit suite.

## Specs and design

- `components.md` — component catalogue with states, props, i18n keys, and artboard cross-reference.
- `docs/superpowers/specs/2026-05-06-mitosis-design-system-design.md` — full spec.
- `research/` — XD deck atlas (59 PNGs), live-site CSS-custom-property snapshot, per-screen catalogue.
- `assets/` — extracted brand logo SVG and the live site's Bluesky logo PNG.

## License

MIT.
````

- [ ] **Step 2: Commit**

```bash
git add design-system/README.md
git commit -m "docs(design-system): add README"
```

---

## Final acceptance check (run after all tasks)

- [ ] **Run the full local CI loop**

```bash
cd ./design-system
pnpm install --frozen-lockfile
node scripts/sync-tokens.mjs --check
pnpm build:mitosis
pnpm test
pnpm typecheck
```

Expected: every step exits 0. The Vitest suite reports ≥ 30 passing tests across `tokens`, `i18n`, `intl`, `password`, and 11 component emit-checks.

- [ ] **Verify the demo app boots**

```bash
pnpm demo
# Open http://localhost:5173 in a browser. All atoms must render. Stop with Ctrl+C.
```

- [ ] **Verify Histoire boots**

```bash
pnpm story
# Open http://localhost:6006 in a browser. 11 stories listed. Stop with Ctrl+C.
```

- [ ] **Confirm CI workflow file is wired**

```bash
git ls-files design-system/.github
# Expected: design-system/.github/workflows/ci.yml
```

- [ ] **Confirm `output/` is gitignored but `output/<target>/src/*` is committed**

The Mitosis emit lives under `output/`. Per the `.gitignore`, only `output/*/dist` and `output/*/node_modules` are excluded — the generated source is committed alongside the `.lite.tsx`. This matters because Mitosis re-emits deterministically; checking the output in catches generator-version drift.

```bash
git status --porcelain | grep '^??' | grep output | head
# Expected: empty (no untracked output files)
```

If untracked output files appear, decide per the project's policy whether to commit them or extend `.gitignore`. The default in this plan is **commit them**.

---

## Self-review notes (already applied during writing)

- **Spec coverage:** every M1 deliverable in spec §9 has at least one task. Tokens (Task 3), i18n (Task 4), Intl (Task 5), password helpers (Task 6), 11 atoms (Tasks 9–19), demo (Task 21), Histoire (Task 22), CI (Task 23), README (Task 24).
- **Type consistency:** `Locale`, `IconName`, `ButtonVariant`, `ErrorMessage` defined once in `src/types.ts` / `src/utils/i18n.ts` and referenced consistently.
- **No placeholders:** every step contains either a code block or a concrete shell command with expected output.
- **Out-of-scope deferrals are explicit:** zxcvbn-ts and Pwned Passwords noted "land in M3"; visual regression "land in M3"; remaining 5 generators "M4". Edit-password add-confirm-field decision captured (it lives in M3 with the auth organism, not M1).

If a step in execution diverges (e.g. Mitosis 0.10 emits Vue slots differently than this plan assumes for `Checkbox.labelChildren`), document the deviation in the commit message and update `CONVENTIONS.md` so M2 atoms inherit the resolution.
